# H5 frontend environment template.
# Production is served under /h5/ and uses /api through Nginx.

VITE_API_BASE_URL=/api
VITE_USE_MOCK=false
VITE_PUBLIC_BASE=/h5/

# Local dev only: vite.config.ts proxies API requests to this backend.
VITE_BACKEND_PROXY_TARGET=http://localhost:8099
